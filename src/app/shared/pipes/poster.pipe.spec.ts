/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PosterPipe } from './poster.pipe';

describe('PosterPipe', () => {
  it('create an instance', () => {
    let pipe = new PosterPipe();
    expect(pipe).toBeTruthy();
  });
});
